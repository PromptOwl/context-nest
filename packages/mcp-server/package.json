{
  "name": "@promptowl/contextnest-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for Context Nest vault access by AI agents",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "contextnest-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@promptowl/contextnest-engine": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsup": "^8.0.0",
    "vitest": "^3.0.0"
  },
  "files": ["dist"],
  "license": "AGPL-3.0"
}
