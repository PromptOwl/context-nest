{
  "name": "@contextnest/mcp-server",
  "version": "0.2.0",
  "description": "ContextNest MCP Server — serve context from local vaults to Claude, Cursor, and other MCP clients",
  "type": "module",
  "bin": {
    "contextnest-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "contextnest",
    "context",
    "ai",
    "knowledge",
    "claude"
  ],
  "author": "PromptOwl",
  "license": "Apache-2.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
