{
  "name": "@contextnest/core",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:engine": "cd engine && npx jest --no-coverage",
    "build:mcp": "cd mcp-server && npm run build"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
